import AuthForm from '../../components/AuthForm';

export const metadata = { title: 'Login ? Creator AI' };

export default function AuthPage() {
  return (
    <div className="py-8">
      <AuthForm />
    </div>
  );
}
